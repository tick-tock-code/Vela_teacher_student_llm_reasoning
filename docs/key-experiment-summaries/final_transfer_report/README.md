# Final Transfer Report

Paper-facing summary for the primary transfer experiment:

1. Train student models to predict `v25_policies` reasoning features.
2. Append predicted reasoning features to the success-prediction pipeline.
3. Evaluate held-out success prediction with F0.5 as the main metric.

The canonical report is `final_transfer_report.md`. Generated reports and exploratory runs live under `docs/experiment-archive`.

# Data

Store canonical, reusable datasets here.

Guidelines:

- keep raw and curated datasets explicit
- version data artifacts when necessary
- do not store temporary or partially generated outputs here

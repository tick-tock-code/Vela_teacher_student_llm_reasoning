# Tests

Place unit and integration tests here as pipeline components are added.

Prefer tests that verify:

- data contract expectations
- teacher/student interface behavior
- evaluation correctness
